export const metadata = {
  title: "Register | TrashTrack",
  description: "Create your TrashTrack account to start reporting and tracking local waste issues. Join the mission for a cleaner, smarter city today.",
}

export default function RegisterLayout({ children }) {
  return <>{children}</>
}