export const metadata = {
  title: "Resolve | TrashTrack",
  description: "Officers can resolve reports efficiently on TrashTrack. Submit necessary data and help maintain community cleanliness.",
}

export default function ResolveLayout({ children }) {
  return <>{children}</>
}