export const metadata = {
  title: "Dashboard | TrashTrack",
  description: "Manage and monitor complaints, track statuses, and contribute to cleaner communities with your personalized TrashTrack dashboard.",
}

export default function DashboardLayout({ children }) {
  return <>{children}</>
}