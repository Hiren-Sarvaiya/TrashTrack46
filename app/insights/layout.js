export const metadata = {
  title: "Insights | TrashTrack",
  description: "Gain actionable insights into waste report trends, resolution performance, and citizen engagement across regions with TrashTrack's analytics dashboard.",
}

export default function InsightsLayout({ children }) {
  return <>{children}</>
}